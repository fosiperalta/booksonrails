//HOME
=render :partial => 'menubars/menubar_account'
%article.content
	%h1#centered ACCOUNT

	%h2#alinea10ypadding2 Edit account data of #{session[:current_user_username]}

	= form_tag user_path(@user), :method => :put do
		= render :partial => 'user_form'
		%br
		#alinea10ypadding2= submit_tag 'Save Changes'